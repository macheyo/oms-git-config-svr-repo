eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: https://order-mgmt-service-registry.herokuapp.com/eureka/
  instance:
    hostname: localhost
